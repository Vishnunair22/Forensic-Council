"use client";

export function GlobalFooter() {
  return (
    <footer className="w-full py-8 px-8 relative z-50 border-t border-white/[0.05] glass-dock">
      <div className="max-w-7xl mx-auto flex justify-center">
        <span className="text-[10px] font-mono text-white/40 tracking-tight leading-relaxed">
          Forensic Council is an academic project and can occasionally make mistakes.
        </span>
      </div>
    </footer>
  );
}
