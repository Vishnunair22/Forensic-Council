---
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified:
  - apps/api/core/auth.py
  - apps/api/core/config.py
  - apps/api/tests/unit/test_auth_unit.py
  - apps/api/tests/unit/test_config_validation.py
autonomous: true
---

# Plan 03-02: Auth Hardening & Static Analysis

This plan ensures security requirements (RS256, AUD validation) are strictly enforced and the codebase passes all linting/type checks.

## Tasks

<task>
<read_first>
- apps/api/core/auth.py
- apps/api/core/config.py
- apps/api/tests/unit/test_auth_unit.py
</read_first>
<action>
Hardening Auth logic and tests:
1. Ensure `apps/api/core/config.py` default `jwt_algorithm` is `"RS256"`.
2. Update `apps/api/core/auth.py` to strictly enforce `aud` validation (remove any `verify_aud: False` remnants).
3. Add tests in `apps/api/tests/unit/test_auth_unit.py` to:
    - Verify token fails if signed with HS256 when RS256 is expected.
    - Verify token fails if `aud` claim is missing or incorrect.
    - Verify RS256 public/private key fallback/validation works.
</action>
<acceptance_criteria>
- `config.py` has `jwt_algorithm = "RS256"`.
- `test_auth_unit.py` passes with 100% coverage on `auth.py`.
</acceptance_criteria>
</task>

<task>
<read_first>
- pyproject.toml
</read_first>
<action>
Run static analysis Audit:
1. Run `uv run ruff check . --fix` to resolve trivial linting issues.
2. Run `uv run pyright .` and resolve any critical type errors in `core/` and `agents/`.
3. Remove any unused imports and dead code flagged by `ruff`.
</action>
<acceptance_criteria>
- `ruff check .` exits 0.
- `pyright .` exits 0 for `apps/api/core/` and `apps/api/agents/`.
</acceptance_criteria>
</task>

<task>
<read_first>
- tests/
</read_first>
<action>
Final Global Coverage Sweep:
1. Run full test suite with coverage.
2. Identify any remaining files below 50% coverage and add basic smoke tests.
3. Ensure global coverage reaches at least 60%.
</action>
<acceptance_criteria>
- `python -m pytest --cov=. --cov-fail-under=60 tests/` passes.
</acceptance_criteria>
</task>

## Verification
- `uv run ruff check .`
- `uv run pyright .`
- `python -m pytest --cov=. --cov-fail-under=60 tests/`
