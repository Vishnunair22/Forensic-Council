---
phase: 01-structural-audit-fix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/api/constants.py
  - apps/api/orchestration/investigation_runner.py
  - apps/api/api/routes/investigation.py
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-06
must_haves:
  truths:
    - "run_investigation_task is in orchestration/investigation_runner.py"
    - "_EXACT_MIME_EXT_MAP is in api/constants.py (fixed out-of-order)"
    - "investigation.py is a thin route handler (<200 lines)"
    - "All existing investigation tests pass"
  artifacts:
    - path: "apps/api/api/constants.py"
      provides: "Common constants, MIME maps, and phrase tables"
    - path: "apps/api/orchestration/investigation_runner.py"
      provides: "Async investigation pipeline orchestration"
---

<objective>
Decompose api/routes/investigation.py (2,526 lines).

1. Extract orchestration logic (run_investigation_task, _wrap_pipeline_with_broadcasts) to orchestration/investigation_runner.py.
2. Extract constants (MIME maps, phrase tables) to api/constants.py.
3. Fix out-of-order definition of _EXACT_MIME_EXT_MAP (STRUCT-06).
4. Reduce investigation.py to a thin FastAPI route handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-structural-audit-fix/01-RESEARCH.md

<interfaces>
From apps/api/api/routes/investigation.py:
- Route: start_investigation (lines 62–421)
- Constants: _EXACT_MIME_EXT_MAP (line 426), _PIPELINE_TASK_PHRASES (446–537)
- Large orchestration logic: _wrap_pipeline_with_broadcasts (538–2235), run_investigation_task (2236–2526)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract constants to api/constants.py and fix order</name>
  <files>apps/api/api/constants.py, apps/api/api/routes/investigation.py</files>
  <read_first>
    - apps/api/api/routes/investigation.py (lines 420–550)
  </read_first>
  <behavior>
    - Test: from api.constants import _EXACT_MIME_EXT_MAP succeeds
  </behavior>
  <action>
Create api/constants.py. Move _EXACT_MIME_EXT_MAP and _PIPELINE_TASK_PHRASES there.
In investigation.py, import them from api.constants.
Ensure imports happen BEFORE start_investigation (fixes name error potential).
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract orchestration to orchestration/investigation_runner.py</name>
  <files>apps/api/orchestration/investigation_runner.py, apps/api/api/routes/investigation.py</files>
  <read_first>
    - apps/api/api/routes/investigation.py (lines 538–2526)
  </read_first>
  <behavior>
    - Test: from orchestration.investigation_runner import run_investigation_task succeeds
  </behavior>
  <action>
Create orchestration/investigation_runner.py. Move _wrap_pipeline_with_broadcasts and run_investigation_task there.
Add necessary imports to investigation_runner.py.
In investigation.py, import run_investigation_task from orchestration.investigation_runner.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify investigation route refactor</name>
  <files>apps/api/api/routes/investigation.py</files>
  <behavior>
    - Test: cd apps/api && uv run pytest tests/integration/test_investigation_start_flow.py -x -q
    - Test: wc -l apps/api/api/routes/investigation.py should be < 200
  </behavior>
  <action>
Run integration tests for the investigation flow.
Verify line count.
  </action>
</task>

</tasks>
