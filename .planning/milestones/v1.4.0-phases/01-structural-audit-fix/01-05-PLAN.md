---
phase: 01-structural-audit-fix
plan: 05
type: execute
wave: 2
depends_on: ["01", "02", "03", "04"]
files_modified:
  - apps/api/api/main.py
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-07
  - STRUCT-08
must_haves:
  truths:
    - "main.py imports _active_tasks from api.routes._session_state (not investigation)"
    - "No legacy infra.persistence imports exist (already verified, but double check)"
    - "All monolithic files identified in RESEARCH.md are now under their respective ceilings"
    - "Full test suite passes"
---

<objective>
Final cleanup and bug fixes for Phase 1.

1. Fix the import bug in main.py: _active_tasks should be imported from _session_state.
2. Verify all monolithic files are decomposed.
3. Verify no circular imports or side effects (STRUCT-08).
4. Run full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-structural-audit-fix/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix main.py import bug</name>
  <files>apps/api/api/main.py</files>
  <read_first>
    - apps/api/api/main.py (lines 270–290)
  </read_first>
  <behavior>
    - Test: uv run python -c "import api.main" exits 0
  </behavior>
  <action>
In api/main.py line 278, change:
  from api.routes.investigation import _active_tasks
to:
  from api.routes._session_state import _active_tasks
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Final structural audit</name>
  <action>
Check line counts of:
- apps/api/api/routes/investigation.py (Goal: < 200)
- apps/api/core/react_loop.py (Goal: < 2000)
- apps/api/agents/arbiter.py (Goal: < 500)
- apps/api/agents/base_agent.py (Goal: < 1500)

Verify no infra.persistence imports remain.
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run full test suite</name>
  <behavior>
    - Test: cd apps/api && uv run pytest tests/ -x -q
  </behavior>
  <action>
Execute the full test suite to ensure structural changes didn't break runtime logic or orchestration.
  </action>
</task>

</tasks>
