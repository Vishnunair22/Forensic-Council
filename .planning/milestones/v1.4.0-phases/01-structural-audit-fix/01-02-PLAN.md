---
phase: 01-structural-audit-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/agents/reflection.py
  - apps/api/agents/base_agent.py
  - apps/api/agents/__init__.py
autonomous: true
requirements:
  - STRUCT-01
  - STRUCT-05
must_haves:
  truths:
    - "SelfReflectionReport is importable from agents.reflection"
    - "_attach_llm_reasoning_to_findings is importable from agents.reflection"
    - "agents.base_agent re-exports SelfReflectionReport and ForensicAgent"
    - "agents/__init__.py re-exports both ForensicAgent and SelfReflectionReport"
    - "Existing unit tests for base_agent and agents pass"
  artifacts:
    - path: "apps/api/agents/reflection.py"
      provides: "Self-reflection data models and reasoning logic"
      exports:
        - SelfReflectionReport
        - _attach_llm_reasoning_to_findings
    - path: "apps/api/agents/base_agent.py"
      provides: "ForensicAgent ABC and lifecycle"
---

<objective>
Extract the self-reflection data model and LLM reasoning attachment logic from
agents/base_agent.py (1,601 lines) into agents/reflection.py.

This reduces the size and complexity of the base agent class by separating the 
reflection data contract and its specific utility functions from the core agent 
lifecycle. Re-exports are maintained to prevent breaking consumers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-structural-audit-fix/01-RESEARCH.md

<interfaces>
From apps/api/agents/base_agent.py (lines 42–71) — moves to agents/reflection.py:
```python
class SelfReflectionReport(BaseModel):
    agent_id: str
    session_id: uuid.UUID
    reasoning_traces: list[str]
    logical_gaps_identified: list[str]
    suggested_refinements: list[str]
    confidence_delta: float
    timestamp: datetime = Field(default_factory=datetime.utcnow)
```

From apps/api/agents/base_agent.py (lines 74–199) — moves to agents/reflection.py:
```python
def _attach_llm_reasoning_to_findings(
    findings: list[AgentFinding], 
    reflection: SelfReflectionReport
) -> list[AgentFinding]:
    # ... function body ...
```

Consumers verified by grep:
- agents/__init__.py re-exports ForensicAgent and SelfReflectionReport
- tests/unit/test_agents.py imports SelfReflectionReport
- tests/unit/test_base_agent_functions.py imports _attach_llm_reasoning_to_findings
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create agents/reflection.py and move symbols</name>
  <files>apps/api/agents/reflection.py, apps/api/agents/base_agent.py, apps/api/agents/__init__.py</files>
  <read_first>
    - apps/api/agents/base_agent.py (lines 1–200)
    - apps/api/agents/__init__.py
  </read_first>
  <behavior>
    - Test 1: from agents.reflection import SelfReflectionReport, _attach_llm_reasoning_to_findings succeeds
    - Test 2: from agents.base_agent import SelfReflectionReport succeeds (re-export)
    - Test 3: from agents import SelfReflectionReport succeeds (init re-export)
  </behavior>
  <action>
Step 1 — Create apps/api/agents/reflection.py:
  Add necessary imports (uuid, datetime, typing, pydantic.BaseModel, core.react_loop.AgentFinding).
  Copy SelfReflectionReport and _attach_llm_reasoning_to_findings from base_agent.py.

Step 2 — Modify apps/api/agents/base_agent.py:
  Remove the moved symbols.
  Import them from .reflection (SelfReflectionReport, _attach_llm_reasoning_to_findings).
  This preserves re-exports for any external consumers importing from base_agent.

Step 3 — Update apps/api/agents/__init__.py:
  Update re-exports if necessary (though if base_agent re-exports, it might already work).
  Recommended: Change `from .base_agent import ForensicAgent, SelfReflectionReport` 
  to:
    from .base_agent import ForensicAgent
    from .reflection import SelfReflectionReport

Step 4 — Verify:
  Run unit tests for agents and base_agent.
  </action>
  <verify>
    <automated>cd apps/api && uv run python -c "from agents.reflection import SelfReflectionReport; print('reflection.py OK')" && uv run python -c "from agents import SelfReflectionReport; print('agents init OK')" && uv run pytest tests/unit/test_agents.py tests/unit/test_base_agent_functions.py -x -q</automated>
  </verify>
</task>

</tasks>
