# Agent Capabilities & Methodologies

The Forensic Council agents utilize a hybrid approach: Large Language Models handle the cognitive ReAct loop (orchestration/deduction), while specialized machine learning scripts perform the mathematical anomaly detection.

## ⚠️ Production Readiness Warning

**CRITICAL**: This analysis reveals that while agents have real ML-based analysis capabilities, several critical tools use **stub/heuristic implementations** that return random or deterministic-but-fake data. These stubs are **NOT court-defensible** and must be replaced with real implementations before production deployment.

### Summary of Issues
- **34 tools**: Real ML-based implementations (74%)
- **12 tools**: Stub/heuristic implementations (26%)
- **4 critical stubs**: Contraband detection, adversarial robustness, reverse image search, astronomical validation
- **6 new tools (v0.8.1)**: `extract_evidence_text`, `mediainfo_profile`, `av_file_identity` added to Agent 1, 4, and 5

---

## Confidence Scoring Methodology

Every finding generated by an agent receives a numerical `confidence_score` (0.0 to 1.0).
1. **Mathematical Baseline**: The subprocess scripts return a raw anomaly score (e.g., `-0.8` from an `IsolationForest`).
2. **LLM Calibration**: The agent's LLM interprets this score against the context (e.g., "The image is highly compressed, so a minor ELA anomaly is expected").
3. **Arbiter Adjustment**: The Council Arbiter may demote a score if a finding is contested by another agent without sufficient proof.

---

## Tool Implementation Status Legend

| Icon | Meaning |
|------|---------|
| ✅ | **Real Implementation** - Uses actual ML models or algorithmic analysis |
| ⚠️ | **Stub/Heuristic** - Returns random or deterministic fake data |
| 🔴 | **Critical Stub** - Has legal/regulatory implications |

---

## 1. Agent 1: Image Forensics

**Primary Role**: Detect pixel-level manipulation, splicing, and generative artifacts.

### Tool Registry

| Tool | Status | Implementation Details |
|------|--------|------------------------|
| `ela_full_image` | ✅ Real | Error Level Analysis on actual image |
| `ela_anomaly_classify` | ✅ Real | IsolationForest ML model |
| `roi_extract` | ✅ Real | Region of Interest extraction |
| `jpeg_ghost_detect` | ✅ Real | JPEG compression analysis |
| `frequency_domain_analysis` | ✅ Real | FFT-based frequency analysis |
| `splicing_detect` | ✅ Real | DCT quantization via ML subprocess |
| `noise_fingerprint` | ✅ Real | PRNU noise analysis via ML subprocess |
| `deepfake_frequency_check` | ✅ Real | GAN artifact detection |
| `file_hash_verify` | ✅ Real | SHA-256 hash verification |
| `perceptual_hash` | ✅ Real | pHash computation |
| `adversarial_robustness_check` | ⚠️ Stub | Returns `rng.choice([True, False, False])` |
| `sensor_db_query` | ⚠️ Stub | Random sensor match data |
| `extract_evidence_text` | ✅ Real | **NEW** Auto-dispatching OCR: PyMuPDF (PDF) → EasyOCR → Tesseract fallback |

### Capabilities

**What it CAN detect**:
*   Items "photoshopped" or spliced into an image from a different source.
*   AI-generated (GAN/Diffusion) images via frequency analysis.
*   Localized cloning/healing brush usage.

**What it CANNOT detect**:
*   Images that have been downscaled, heavily compressed (JPEG quality < 50), and printed/rescanned.
*   Adversarial attacks designed to evade ELA (stub implementation).

---

## 2. Agent 2: Audio & Multimedia

**Primary Role**: Identify audio deepfakes, synthetic voices, and audio splicing.

### Tool Registry

| Tool | Status | Implementation Details |
|------|--------|------------------------|
| `speaker_diarize` | ✅ Real | Speaker diarization |
| `anti_spoofing_detection` | ✅ Real | Anti-spoofing ML model |
| `prosody_analysis` | ✅ Real | Prosody feature extraction |
| `audio_splice_detect` | ✅ Real | ML-based splice detection |
| `background_noise_analysis` | ✅ Real | Noise consistency analysis |
| `codec_fingerprinting` | ✅ Real | Codec identification |
| `audio_visual_sync` | ⚠️ Stub | Random sync check result |
| `inter_agent_call` | ⚠️ Stub | Mock response |
| `adversarial_robustness_check` | ⚠️ Stub | Random boolean + confidence |

### Capabilities

**What it CAN detect**:
*   Sudden dropouts or shifts in background room tone (indicating a splice).
*   AI-generated voices lacking natural respiratory micro-pauses.

**What it CANNOT detect**:
*   Audio-visual synchronization issues (stub implementation).
*   Mimicry by a professional human impersonator.
*   Tampering in audio files with SNR below 10dB.

---

## 3. Agent 3: Object Detection

**Primary Role**: Contextual validation of items within the scene.

### Tool Registry

| Tool | Status | Implementation Details |
|------|--------|------------------------|
| `object_detection` | ✅ Real | YOLO-based detection on actual image |
| `secondary_classification` | ✅ Real | Secondary classification pass |
| `scale_validation` | ✅ Real | Scale analysis using EXIF |
| `lighting_consistency` | ✅ Real | OpenCV-based lighting analysis |
| `scene_incongruence` | ✅ Real | OpenCV quadrant noise analysis |
| `image_splice_check` | ✅ Real | DCT-based splicing detection |
| `noise_fingerprint` | ✅ Real | PRNU analysis |
| `contraband_database` | 🔴 **CRITICAL STUB** | **Random 25% match - FAKE!** |
| `inter_agent_call` | ⚠️ Stub | Mock response |
| `adversarial_robustness_check` | ⚠️ Stub | Random boolean |

### ⚠️ Critical Issue: Contraband Database is FAKE

```python
async def contraband_database(input_data: dict) -> dict:
    has_hit = rng.choice([False, False, False, True])  # Random 25% match!
```

**Legal Impact**: This stub could incorrectly flag innocent images as containing weapons/contraband, or miss actual threats. **Must be replaced with real database integration (e.g., FBI NIBIN, Interpol)** before production.

### Capabilities

**What it CAN detect**:
*   Lighting/shadow directional inconsistencies across objects.
*   Scale/perspective anomalies.

**What it CANNOT detect**:
*   Actual weapons/contraband (fake database results).
*   Perfectly rendered 3D objects with matched HDRI lighting.

---

## 4. Agent 4: Video Forensics

**Primary Role**: Analyze temporal inconsistency in moving images.

### Tool Registry

| Tool | Status | Implementation Details |
|------|--------|------------------------|
| `optical_flow_analysis` | ✅ Real | Dense optical flow computation |
| `frame_extraction` | ✅ Real | Frame window extraction |
| `frame_consistency_analysis` | ✅ Real | Frame-to-frame analysis |
| `face_swap_detection` | ✅ Real | Deepfake face detection |
| `video_metadata` | ✅ Real | Metadata extraction |
| `frequency_gan_detect` | ⚠️ Stub | Random GAN detection |
| `anomaly_classification` | ⚠️ Stub | Random classification |
| `rolling_shutter_validation` | ⚠️ Stub | Random validation |
| `inter_agent_call` | ⚠️ Stub | Mock response |
| `adversarial_robustness_check` | ⚠️ Stub | Random boolean |
| `mediainfo_profile` | ✅ Real | **NEW** Deep AV container profiling: codec, VFR flag, encoding tools, forensic flags |
| `av_file_identity` | ✅ Real | **NEW** Lightweight container pre-screen: format, codec, high-severity flags |

### Capabilities

**What it CAN detect**:
*   Face-swapping via facial-boundary blending analysis.
*   Missing frames via timestamp/motion-vector mismatch.

**What it CANNOT detect**:
*   Proper anomaly classification (explainable vs suspicious) - stub.
*   Rolling shutter violations - stub.
*   Extremely low framerate footage tampering.

---

## 5. Agent 5: Metadata Forensics

**Primary Role**: Structural and contextual validation of the file container.

### Tool Registry

| Tool | Status | Implementation Details |
|------|--------|------------------------|
| `exif_extract` | ✅ Real | Full EXIF extraction |
| `metadata_anomaly_score` | ✅ Real | IsolationForest ML model |
| `gps_timezone_validate` | ✅ Real | GPS coordinate validation |
| `steganography_scan` | ✅ Real | LSB steganography detection |
| `file_structure_analysis` | ✅ Real | File structure forensics |
| `hex_signature_scan` | ✅ Real | Hex signature detection |
| `timestamp_analysis` | ✅ Real | Timestamp consistency |
| `file_hash_verify` | ✅ Real | SHA-256 verification |
| `astronomical_api` | 🔴 **CRITICAL STUB** | **Random sun position - FAKE!** |
| `reverse_image_search` | 🔴 **CRITICAL STUB** | **Random 33% match - FAKE!** |
| `device_fingerprint_db` | 🔴 **CRITICAL STUB** | **Random device matches - FAKE!** |
| `adversarial_robustness_check` | ⚠️ Stub | Random boolean |
| `extract_evidence_text` | ✅ Real | **NEW** PDF→PyMuPDF lossless / Image→EasyOCR→Tesseract |
| `mediainfo_profile` | ✅ Real | **NEW** Deep AV container profiling: codec, encoding tool, VFR flag, forensic flags |
| `av_file_identity` | ✅ Real | **NEW** Lightweight AV pre-screen: format, codec, high-severity flags |

### ⚠️ Critical Issues

#### 1. Astronomical API (FAKE)
```python
sun_position_valid = rng.choice([True, False, True, True])  # Random
```
**Impact**: Cannot validate if sun position matches claimed GPS/timestamp.

#### 2. Reverse Image Search (FAKE)
```python
has_match = rng.choice([False, False, True])  # Random 33%
```
**Impact**: Cannot verify if evidence appeared online before.

#### 3. Device Fingerprint Database (FAKE)
```python
device_model_matched = rng.choice([True, False])  # Random 50%
```
**Impact**: Cannot verify if device model in EXIF is authentic.

### Capabilities

**What it CAN detect**:
*   Software signatures (Photoshop, Premiere Pro).
*   Mismatched creation vs modification dates.
*   GPS contradictions via timezone validation.

**What it CANNOT detect**:
*   Actual prior online existence of image.
*   Real astronomical validation.
*   Authentic device fingerprint verification.

---

## Recommendations for Production

### Before Production Deployment

1. **Replace Critical Stubs Immediately**:
   - Integrate with FBI NIBIN or Interpol for contraband detection
   - Use Google Images API or TinEye for reverse image search
   - Integrate NOAA/SunCalc API for astronomical validation
   - Connect to CameraV or similar for device fingerprints

2. **Add Tool Confidence Annotation**:
   ```json
   {
     "finding": "...",
     "tool_confidence": "real_implementation" | "stub_heuristic",
     "court_defensible": true | false
   }
   ```

3. **Disable Non-Defensible Tools in Production Mode**:
   Add configuration flag to disable stub tools when `APP_ENV=production`.

### Post-Production Improvements

1. Implement real adversarial robustness testing
2. Build proper rolling shutter validation
3. Create anomaly classification models
4. Add circuit breakers for external APIs

---

## Quick Reference: Stub Tools Summary

| Agent | Stub Tool | Risk Level | Required Fix |
|-------|-----------|------------|--------------|
| Agent 1 | `adversarial_robustness_check` | High | Implement adversarial testing |
| Agent 1 | `sensor_db_query` | High | Integrate camera sensor DB |
| Agent 2 | `audio_visual_sync` | High | Implement AV sync detection |
| Agent 2 | `adversarial_robustness_check` | High | Implement adversarial testing |
| Agent 3 | `contraband_database` | **Critical** | Integrate FBI/Interpol DB |
| Agent 3 | `adversarial_robustness_check` | High | Implement adversarial testing |
| Agent 4 | `anomaly_classification` | High | Build classification model |
| Agent 4 | `rolling_shutter_validation` | High | Implement RS validation |
| Agent 4 | `adversarial_robustness_check` | High | Implement adversarial testing |
| Agent 5 | `astronomical_api` | **Critical** | Integrate SunCalc/NOAA |
| Agent 5 | `reverse_image_search` | **Critical** | Integrate Google Images API |
| Agent 5 | `device_fingerprint_db` | **Critical** | Integrate CameraV DB |
| Agent 5 | `adversarial_robustness_check` | High | Implement adversarial testing |

---

## Production Readiness Verdict

**Status: ⚠️ NOT PRODUCTION-READY for Legal Use**

While the system has strong ML-based pixel, audio, and temporal analysis capabilities, **4 critical stub implementations that affect legal determinations must be replaced before any court use**:

1. Contraband/Weapons detection (random results)
2. Reverse image search (random results)
3. Astronomical validation (random results)
4. Device fingerprint verification (random results)

**Estimated Fix Timeline**: 2-3 weeks for external API integrations.
