import { EvidenceUploadClient } from "@/components/pages/EvidenceUploadClient";

export const dynamic = "force-dynamic";

export default function EvidenceUploadPage() {
  return <EvidenceUploadClient />;
}