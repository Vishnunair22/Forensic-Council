import { SessionExpiredClient } from "@/components/pages/SessionExpiredClient";

export const dynamic = "force-dynamic";

export default function SessionExpiredPage() {
  return <SessionExpiredClient />;
}