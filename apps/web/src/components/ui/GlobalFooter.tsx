"use client";

export function GlobalFooter() {
  return (
    <footer className="w-full py-12 text-center px-6 relative z-50 border-t border-white/[0.02] mt-12 bg-gradient-to-t from-black to-transparent">
        <p className="text-[11px] font-medium text-white/30 hover:text-white/50 transition-colors duration-300 tracking-wider max-w-2xl mx-auto leading-relaxed font-sans">
          Forensic Council is an academic project and can occasionally make mistakes.
        </p>
    </footer>
  );
}
