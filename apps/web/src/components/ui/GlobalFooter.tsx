"use client";

export function GlobalFooter() {
  return (
    <footer className="w-full py-16 border-t border-border-subtle bg-surface-low/30 backdrop-blur-xl text-center px-6 relative z-50">
        <p className="text-[9px] font-mono font-medium text-white/30 tracking-[0.1em] uppercase max-w-2xl mx-auto leading-relaxed">
          Forensic Council is an academic project and can occasionally make mistakes.
        </p>
    </footer>
  );
}
